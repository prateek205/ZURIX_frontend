import React from "react";
import ProfileSection from "../components/ProfileSection";

const Profile = () => {
  return (
    <section>
      <ProfileSection />
    </section>
  );
};

export default Profile;
