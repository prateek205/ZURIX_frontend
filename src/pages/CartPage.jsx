import React from "react";
import CartSection from "../components/carts/CartSection";

const CartPage = () => {
  return (
    <section>
      <CartSection />
    </section>
  );
};

export default CartPage;
